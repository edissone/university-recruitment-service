spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.HSQLDialect
    database: H2
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:recruitment_db
    username: sa
    password: sa
  flyway:
    enabled: false


